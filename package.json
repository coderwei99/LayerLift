{
  "name": "layerlift-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "publish": "pnpm -r publish --access public"
  },
  "devDependencies": {
    "pnpm": "^8.0.0"
  }
}